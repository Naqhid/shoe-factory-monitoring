{
  "scripts": {
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "start:prod": "npm run build --prefix frontend && npm start --prefix backend",
    "dev:backend": "npm start --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently -n api,web \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:legacy": "vite --host 0.0.0.0",
    "tunnel:login": "cloudflared\\cloudflared.exe tunnel login",
    "tunnel:create": "cloudflared\\cloudflared.exe tunnel create shoe-factory",
    "tunnel:route": "cloudflared\\cloudflared.exe tunnel route dns shoe-factory shoe-factory.yourdomain.com",
    "tunnel": "cloudflared\\cloudflared.exe tunnel --config cloudflared\\config.yml run shoe-factory",
    "dev:tunnel": "concurrently \"npm run dev\" \"cd backend && npm start\" \"npm run tunnel\""
  },
  "dependencies": {
    "jspdf": "^4.2.1",
    "jspdf-autotable": "^5.0.8",
    "react-qr-scanner": "^1.0.0-alpha.11"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
